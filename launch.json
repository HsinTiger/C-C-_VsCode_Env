{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(lldb）debugger",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/${fileBasenameNoExtension}",
      "args": [],
      "stopAtEntry": true,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": true,
      "MIMode": "lldb",
      "preLaunchTask": "Build with Clang++",
      "logging": {
        "trace": true,
        "traceResponse": true,
        "engineLogging": true
      }
    },
  ]
}